# Список промт-запросов (ТЗ)

Ниже собраны основные запросы к ассистенту по проекту `gbc-tz` в едином формате.

## Сводка: что просили по Supabase и Vercel

Эти темы раньше были разнесены по разным пунктам списка; здесь — одним местом.

### Supabase

- Сгенерировать **SQL** для таблицы `rcrm_orders` по полям из `mock_orders.json` (быстро поднять схему в Supabase).
- Написать **PHP-скрипт** `import_retailcrm_to_supabase.php`: тянуть заказы из RetailCRM (`GET /api/v5/orders`), маппить в колонки таблицы, писать через Supabase REST API; учесть пагинацию (`limit` только 20 / 50 / 100).
- Добавить режим **`--dry-run`**: показать список полей и пример строки **без записи** в БД; поправить пример под ограничения API (в т.ч. `custom_fields` как объект `{}`).

### Vercel

- Подготовить **дашборд** в `vercel_dashboard/`: статика (`index.html`, Chart.js) + **serverless** `api/stats.js`, который читает агрегаты из Supabase по REST; переменные окружения на Vercel (`SUPABASE_URL`, ключ).
- После **переноса** папки дашборда — проверить целостность и пути (`@vercel_dashboard`).
- Описать всё в **README** для GitHub (в т.ч. блоки про Supabase и деплой на Vercel).

---

1. **Запрос:**  
   Итак наш план такой. Сначала нужно сделать скрипт который заберет заказы из `W:\gbc-tz\mock_orders.json` и зальет в retailCRM. Пример API метода заказы - `@inst.txt`. Следует сделать на PHP. Планируется выполнение по крону, проверь чтобы в спецификации API при загрузке не было дублей заказов. Также опиши, все ли поля из JSON можно по умолчанию положить в карточку заказа (требуется ли метод отдельный для создания клиента и дальнейшего связывания). Приступай. Сначала жду ответ.

2. **Запрос:**  
   `@inst_upload_order.md` — вот пример метода создания заказа `POST /api/v5/orders/create`.

3. **Запрос:**  
   «вытащи мне поля основные отюда `@mock_orders.json (2-26)` и сделай SQL, чтобы я мог быстро таблицу в `supabase.com` создать (буду писать заказы). Таблица `rcrm_orders`. Выведи структуру просто в чат».

4. **Запрос:**  
   «это тестовый пример. давай пока откажемся от env. напиши просто в коде данные `https://aliniym.retailcrm.ru` `P19fgkeVHqavN7MO3sofGu4xhMy9ADSv` — токен».

5. **Запрос:**  
   «финально давай проверим все ли соответствует `@mock_orders.json` (все поля запишем)? я создал кастомное поле для `utm_source` и тип заказа».

6. **Запрос:**  
   `@import_orders.log (39-43)`.

7. **Запрос:**  
   «Отлично. сделай мне документ с списком моих промт запросов к тебе. Я делаю это как ТЗ».

8. **Запрос:**  
   Второй скрипт: загрузить данные в Supabase; таблица создана по SQL; данные брать из API RetailCRM; пример GET-запроса — `@inst.txt`.

9. **Запрос:**  
   «Сделай режим в скрипте чтобы просто показал какие поля отправит в БД (без записи)».

10. **Запрос:**  
   Разбор вывода терминала: `--dry-run`, ошибка пагинации RetailCRM (`limit` только 20/50/100), корректность `custom_fields` в примере.

11. **Запрос:**  
   `@vercel_dashboard` — по дашборду переместил всё в эту папку; проверить целостность и правильность путей.

12. **Запрос:**  
   Для Telegram нужен простой скрипт: принять хук из RetailCRM с параметром в теле (стоимость) и отправить сообщение в Telegram-чат.

13. **Запрос:**  
   Скрипт удалён; нужен обычный PHP-скрипт под свой сервер, PHP 7.4, в папке `W:\gbc-tz`.

14. **Запрос:**  
   «Давай лучше будем GET принимать с полями; тут есть пример `W:\davYandex`».

15. **Запрос:**  
   «Сделай README для GitHub обо всём, что есть в `W:\gbc-tz`».

16. **Запрос:**  
   «Дополни `W:\gbc-tz\prompts_tz.md` основными моими запросами».

17. **Запрос (Supabase — разработка скриптов и схемы):**  
   Сделать цепочку под Supabase: SQL для `rcrm_orders`, затем скрипт выгрузки из RetailCRM в эту таблицу, режим просмотра полей без записи, соответствие ограничениям API пагинации.

18. **Запрос (Vercel — дашборд и деплой):**  
   Собрать фронт + serverless API для статистики из Supabase, задеплоить на Vercel, проверить пути после переноса папки, задокументировать в README.

